database:
  name: carrpigeon
  host: postgres
  port: 5432
  user: carrpigeon_user
  password: carrpigeon_9876
  sslmode: disable
  maxOpenConns: 10
  maxIdleConns: 10
  maxIdleTime: 15m
httpServer:
  address: 0.0.0.0:2500
  idleTimeout: 90s
  readTimeout: 10s
  writeTimeout: 20s
  cors:
    allowedOrigins:
      - http://localhost:3000
      - http://localhost:5173
      - http://localhost:8080
smtp:
  host: smtp.gmail.com
  port: 587
  user: [EMAIL_ADDRESS]
  password: [PASSWORD]
logger:
  level: info # debug, info, warn, error
